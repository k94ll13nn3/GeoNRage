@page "/{Id:int}"

<Container Condition="IsConnected && Game is not null">
    <Title Value="@($"GeoNRage — #{Game!.Id}")"></Title>

    <h1>#@Game!.Id — @Game!.Name</h1>

    <table>
        <Repeater Items="Game.Maps">
            <ItemTemplate Context="map">
                <tr>
                    <th>@map</th>
                    <Repeater Items="Game.Columns">
                        <ItemTemplate Context="column">
                            <th>@column</th>
                        </ItemTemplate>
                    </Repeater>
                </tr>

                <Repeater Items="Game.Rows">
                    <ItemTemplate Context="row">
                        <tr>
                            <th>@row</th>
                            <Repeater Items="Game.Columns">
                                <ItemTemplate Context="column">
                                    @{
                                        var k = $"{map}_{column}_{row}";
                                    }
                                    <td><input type="number" value="@Game.Values[k]" @onchange="@((e) => { Send(k, e);})" /></td>
                                </ItemTemplate>
                            </Repeater>
                        </tr>
                    </ItemTemplate>
                </Repeater>

                <tr>
                    <th>Total @map</th>
                    <Repeater Items="Game.Columns">
                        <ItemTemplate Context="column">
                            <td>@Game.Values.Where(x => x.Key.StartsWith($"{map}_{column}")).Sum(x => x.Value)</td>
                        </ItemTemplate>
                    </Repeater>
                </tr>
            </ItemTemplate>
        </Repeater>

        <tr style="border-bottom: 1px solid #000;"></tr>

        <tr class="bordered">
            <th>Total </th>
            <Repeater Items="Game.Columns">
                <ItemTemplate Context="column">
                    <td>@Game.Values.Where(x => x.Key.Contains($"_{column}")).Sum(x => x.Value)</td>
                </ItemTemplate>
            </Repeater>
        </tr>
    </table>
</Container>
