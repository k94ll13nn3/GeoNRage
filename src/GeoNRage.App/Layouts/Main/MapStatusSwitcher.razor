<label class="mr-3">
    @(AllMaps ? "Toutes cartes" : "Cartes principales")
</label>
<label class="switch">
    <input type="checkbox" @onchange="ChangeMapStatusAsync" checked="@AllMaps">
    <span class="slider"></span>
</label>
