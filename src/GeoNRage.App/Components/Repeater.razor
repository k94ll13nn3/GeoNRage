@typeparam T

@if (Items?.Any() == true && ChildContent is not null)
{
    foreach (var item in Items)
    {
        @ChildContent(item);
    }
}

@code {
    [Parameter]
    public IEnumerable<T>? Items { get; set; }

    [Parameter]
    public RenderFragment<T>? ChildContent { get; set; }
}
