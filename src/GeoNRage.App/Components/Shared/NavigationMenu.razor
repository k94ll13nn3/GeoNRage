<div class="menu-links">
    <NavLink Match="NavLinkMatch.Prefix" href="@NavigationManager.ToAbsoluteUri("/games")" class="ml-10 mr-10">Parties</NavLink>
    <NavLink Match="NavLinkMatch.Prefix" href="@NavigationManager.ToAbsoluteUri("/statistics")" class="ml-10 mr-10">Stats</NavLink>
    <AuthorizeView>
        —
        <NavLink Match="NavLinkMatch.Prefix" href="@NavigationManager.ToAbsoluteUri("/admin/maps")" class="ml-10 mr-10">Cartes (admin)</NavLink>
        <NavLink Match="NavLinkMatch.Prefix" href="@NavigationManager.ToAbsoluteUri("/admin/players")" class="ml-10 mr-10">Joueurs (admin)</NavLink>
        <NavLink Match="NavLinkMatch.Prefix" href="@NavigationManager.ToAbsoluteUri("/admin/games")" class="ml-10 mr-10">Parties (admin)</NavLink>
        <NavLink Match="NavLinkMatch.Prefix" href="@NavigationManager.ToAbsoluteUri("/admin/import")" class="ml-10 mr-10">Import (admin)</NavLink>
    </AuthorizeView>

    <div class="theme-chooser">
        <AuthorizeView>
            <Authorized>
                @Name
                <button type="button" @onclick="@LogoutClickAsync">Déconnexion</button>
            </Authorized>
            <Authorizing>
                <GeoNRage.App.Components.Admin.Login />
            </Authorizing>
            <NotAuthorized>
                <GeoNRage.App.Components.Admin.Login />
            </NotAuthorized>
        </AuthorizeView>

        <select @onchange="ChangeThemeAsync">
            <option value="basic">Basic</option>
            <option value="legacy">Legacy</option>
            <option value="zelda">Zelda</option>
        </select>
    </div>
</div>
